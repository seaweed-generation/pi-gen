if [[ -z "$DEVICE_ID" ]]; then
  DEVICE_ID="NEW"
fi

IMG_NAME=algos-amd64
RELEASE=bullseye
TARGET_HOSTNAME="seagen-${DEVICE_ID}.local"
LOCALE_DEFAULT=en_US.UTF-8
KEYBOARD_KEYMAP=us
TIMEZONE_DEFAULT="Etc/UTC"
DISABLE_FIRST_BOOT_USER_RENAME=1
ENABLE_SSH=1
