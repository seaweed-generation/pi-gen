IMG_NAME=algos-arm64
RELEASE=bullseye
TARGET_HOSTNAME="seagen-extender.local"
WPA_COUNTRY=US
LOCALE_DEFAULT=en_US.UTF-8
KEYBOARD_KEYMAP=us
TIMEZONE_DEFAULT="Etc/UTC"
DISABLE_FIRST_BOOT_USER_RENAME=1
ENABLE_SSH=1

DEPLOY_COMPRESSION=zip
IMG_DATE=$(date +%Y-%m-%d)
IMG_FILENAME="${IMG_DATE}-${IMG_NAME}"
ARCHIVE_FILENAME="image_${IMG_FILENAME}"
